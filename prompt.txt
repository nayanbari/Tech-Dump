Act as an expert Spring Boot developer. I need to modify my existing Spring Boot application (running on Server S1) to emit structured JSON logs. These logs will be streamed in real-time to a separate backend aggregator (Server S2) operating within a secure, isolated private network.
Task Requirements:
1. Dependency Management:
Please provide the configuration to add the ⁠net.logstash.logback:logstash-logback-encoder⁠ dependency to our ⁠pom.xml⁠ or ⁠build.gradle⁠. This library is the standard mechanism for generating JSON logs. (Note: If we are migrating to Spring Boot 3.4 or higher, please provide the alternative native configuration using ⁠logging.structured.format.console=ecs⁠ instead of adding external dependencies).
2. Context Enrichment (MDC):
Write a sample Servlet Filter that generates a unique ⁠traceId⁠ for incoming HTTP requests and injects it into the Mapped Diagnostic Context (MDC). Ensure the logging configuration automatically includes this ⁠traceId⁠ field in the final JSON log output so we can trace requests across the UI.
3. Logback Configuration (⁠logback-spring.xml⁠):
Please generate a complete ⁠logback-spring.xml⁠ file that defines two distinct appenders using Spring Profiles so we can toggle between them:
 Profile 1: Direct TCP Appender (Stateless Streaming)
Configure a ⁠LogstashTcpSocketAppender⁠ to push the JSON logs directly over a raw TCP socket to a remote destination (e.g., ⁠SERVER_S2_IP:9000⁠). Use the ⁠LogstashEncoder⁠ to ensure the payload is correctly formatted.
 Profile 2: JSON File Appender (Stateful Tailing)
Configure a ⁠RollingFileAppender⁠ equipped with a ⁠TimeBasedRollingPolicy⁠ to write the JSON logs to local disk (e.g., ⁠/var/log/app/application.json⁠). It must output strict JSON using ⁠LogstashEncoder⁠ so that an open-source log shipper (like Vector) can cleanly tail the file without relying on text parsing.
4. Environment Variable Injection:
Ensure the ⁠logback-spring.xml⁠ uses ⁠<springProperty>⁠ tags to dynamically load the application name, version, and the target TCP destination host/port from the ⁠application.yml⁠ or environment variables, avoiding any hardcoded IP addresses in the XML.
