Here is the step-by-step prompt sequence designed for your AI coding assistant (like GitHub Copilot or ChatGPT) to build the backend.
As an expert prompt engineer, I have structured these prompts to account for your strict Python 3.6 and SQLite constraints. Because modern AI frameworks (like LangChain) and official LLM SDKs have dropped Python 3.6 support, these prompts instruct the AI to build custom, dependency-free implementations.
Copy and paste these prompts sequentially into your coding assistant:
### Prompt 1: Foundation & High-Concurrency SQLite Setup
**Copy this into your Copilot:**
> "Act as an expert Python 3.6 backend developer. We are building the backend for an AI-powered SWIFT synthetic test data generator.
> Task 1: Set up the database connection using Python's built-in sqlite3 module. Since SQLite relies on database-level locking which blocks concurrent readers and writers, you must configure the connection to use Write-Ahead Logging (WAL) mode by executing PRAGMA journal_mode=WAL; upon connection. Also, set a high timeout value (e.g., 20 seconds) to mitigate write-contention lockouts.
> Task 2: Create the schema. We need tables for chat_sessions (to store active user contexts), script_iterations (to store the Python code generated by the LLM), and system_logs. Ensure all timestamps use UTC."
> 
### Prompt 2: Atlassian Jira & Confluence Integration
**Copy this into your Copilot:**
> "Task: Implement the Atlassian extraction module using the requests library (ensure compatibility with Python 3.6).
> Requirement 1: Create a function to extract Jira requirements. Use the GET /rest/api/2/issue/{issueIdOrKey} endpoint to fetch issue details and acceptance criteria.[1]
> Requirement 2: Create a function to extract Confluence data dictionaries. Use the Confluence Cloud REST API v2. Specifically, hit the GET /wiki/api/v2/pages/{id} endpoint. You must append the query parameter ?body-format=storage to retrieve the underlying XML representation of the page.[2, 3]
> Requirement 3: Write a lightweight XML parser using Python's built-in xml.etree.ElementTree to strip out the HTML/XML tags from the Confluence storage format and return clean, raw text for our LLM context."
> 
### Prompt 3: LLM REST API Integration (Bypassing SDK Limits)
**Copy this into your Copilot:**
> "Task: Build the LLM communication layer. Since the official OpenAI and Anthropic Python SDKs require Python 3.7+, we cannot use them in our Python 3.6 environment.
> Write a custom HTTP client class using the requests module to call the LLM's REST API directly (e.g., https://api.openai.com/v1/chat/completions). Implement robust error handling, exponential backoff for rate limits (HTTP 429), and HTTP timeout handling. The class should accept a system prompt, a list of message dictionaries (role and content), and return the raw text response."
> 
### Prompt 4: SWIFT ISO 20022 XML Builder
**Copy this into your Copilot:**
> "Task: Create a utility script that the LLM can import to safely generate ISO 20022 XML data (like pacs.008 messages). Because modern financial libraries like pain001 or pyiso20022 are not supported on Python 3.6, we must build a manual constructor.
> Use Python's built-in xml.etree.ElementTree module. Create a Python class ISO20022Builder. It should include methods to construct the <AppHdr> block and the nested <pacs.008> body. Include specific functions for creating complex nested structures required for SWIFT, such as the Group Header and the Credit Transfer Transaction Information blocks.[4] Ensure the class outputs well-formed XML strings without using external libraries."
> 
### Prompt 5: The Recursive Evaluator-Refiner Loop
**Copy this into your Copilot:**
> "Task: Implement the core AI cognitive architecture using an Evaluator-Refiner loop.[5]
> Step 1 (Generation): The backend takes the user's prompt, injects any Jira/Confluence context, and calls our LLM REST client to generate a Python data-synthesis script.
> Step 2 (Execution): Save the script locally and use Python's subprocess.run() to execute it. Capture both stdout and stderr.
> Step 3 (Evaluation): If stderr contains errors, automatically pass the error traceback back to the LLM REST client, prompting it to act as an independent evaluator to fix the bug.[5]
> Step 4 (Limits): Hard-cap this automated self-correction cycle at 4 iterations to prevent infinite hallucination loops.[5] Manage the chat history in the SQLite database, ensuring only the most recent functional script is kept in the active LLM context to prevent token overflow."
> 
### Prompt 6: High-Throughput Bulk Generation Pipeline
**Copy this into your Copilot:**
> "Task: Transition the approved data generation script into a bulk processing engine.
> Implement a decoupled Producer/Consumer design pattern [6] compatible with Python 3.6.
>  1. The Producer function should generate a list of randomized input parameters (e.g., varying debtor names and transaction amounts).
>  2. Create a pool of Consumer worker processes using Python's multiprocessing module or a lightweight queue system.
>  3. To prevent severe write-contention on our single SQLite file, the Consumer workers must NOT write the generated millions of synthetic records into the SQLite database. Instead, have them write directly to a local partitioned CSV or XML file on the host filesystem."
> 
